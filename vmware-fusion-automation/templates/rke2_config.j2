{%- if lb_ip %}
server: https://{{ lb_ip }}:9345
{%- endif %}
{%- if backends %}
token-file: /etc/rancher/rke2/token
tls-san:
  {%- for backend in backends %}
  - {{ backend }}
  {%- endfor %}
node-taint:
  - "CriticalAddonsOnly=true:NoExecute"
{%- endif %}
agent-token-file: /etc/rancher/rke2/token
{%- if worker_type %}
node-label:
  - "node-role.kubernetes.io/worker="
  - "worker-type={{ worker_type }}"
{%- endif %}
debug: true
